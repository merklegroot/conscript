<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <!-- Injected into BuildInfo.Timestamp and shown in the top bar -->
    <BuildTimestamp>$([System.DateTime]::Now.ToString("MMM d, yyyy h:mm tt"))</BuildTimestamp>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <RootNamespace>Conscript</RootNamespace>
    <ApplicationIcon>..\assets\icons\conscript.ico</ApplicationIcon>
  </PropertyGroup>

  <ItemGroup>
    <AssemblyAttribute Include="System.Reflection.AssemblyMetadataAttribute">
      <_Parameter1>BuildTimestamp</_Parameter1>
      <_Parameter2>$(BuildTimestamp)</_Parameter2>
    </AssemblyAttribute>
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Facepunch.Steamworks" Version="2.3.3" />
    <PackageReference Include="Raylib-cs" Version="5.0.0" />
    <PackageReference Include="Microsoft.Extensions.Hosting" Version="8.0.0" />
  </ItemGroup>

  <!-- Background images (and future scene art) are embedded resources -->
  <ItemGroup>
    <EmbeddedResource Include="img/**/*" />
  </ItemGroup>

  <!-- Fonts remain loose files in output (easy to hot-swap during development) -->
  <ItemGroup>
    <None Include="Fonts\**\*" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

  <!-- Dev only: Steam looks for this next to the exe when not launched via Steam -->
  <ItemGroup>
    <None Include="steam_appid.txt" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
